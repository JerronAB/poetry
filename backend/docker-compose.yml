services:
  cloudflare-tunnel:
    image: cloudflare/cloudflared:latest
    container_name: mntn-cf-tunnel
    depends_on:
      - 'generator'
    command:
      - tunnel
      - --no-autoupdate
      - run
      - --token
      - ${CLOUDFLARE_TOKEN}
    restart: unless-stopped

  logger:
    image: python:3.14-slim-alpine
    container_name: heartbeat-logger
    volumes:
      - .:/app
    working_dir: /app
    command: python -OO heartbeat_logger.py
    restart: unless-stopped

  simplefileserver:
    build:
      context: ./simplefileserver
    container_name: simplefileserver
    restart: always